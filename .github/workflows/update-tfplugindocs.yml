name: Update tfplugindocs

on:
  workflow_call:
    secrets:
      GH_TOKEN:
        description: Token used to check out, push branches, and open pull requests.
        required: true

permissions:
  contents: write
  pull-requests: write

env:
  TOOL: tfplugindocs
  LINT_FILE: .github/workflows/lint.yml
  MAKEFILE: Makefile

jobs:
  tfplugindocs:
    name: tfplugindocs
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Resolve Latest Version
        run: |
          version="$(
            curl -sf https://proxy.golang.org/github.com/hashicorp/terraform-plugin-docs/@latest |
              jq -r '.Version // empty'
          )"
          [ -n "${version}" ] || {
            printf 'failed to resolve %s version\n' "${TOOL}" >&2
            exit 1
          }
          printf 'VERSION=%s\n' "${version}" >>"${GITHUB_ENV}"

      - name: Bump Lint Workflow
        id: bump-lint
        uses: craigsloggett/bump-version@85e673033cf965281d41ec89c94189a335fa7633 # v1.1.1
        with:
          file: ${{ env.LINT_FILE }}
          key: >-
            .jobs.tfplugindocs.steps[] |
            select(.run | test("tfplugindocs")) |
            .env.TFPLUGINDOCS_VERSION
          version: ${{ env.VERSION }}

      - name: Bump Makefile
        id: bump-makefile
        uses: craigsloggett/bump-version@85e673033cf965281d41ec89c94189a335fa7633 # v1.1.1
        with:
          file: ${{ env.MAKEFILE }}
          key: TFPLUGINDOCS_VERSION
          version: ${{ env.VERSION }}

      - name: Get Changed Files
        id: changed-files
        if: steps.bump-lint.outputs.changed == 'true' || steps.bump-makefile.outputs.changed == 'true'
        env:
          LINT_CHANGED: ${{ steps.bump-lint.outputs.changed }}
          MAKEFILE_CHANGED: ${{ steps.bump-makefile.outputs.changed }}
        run: |
          # shellcheck disable=SC2016 # Backticks here are literal Markdown code-spans.
          {
            printf 'files<<EOF\n'
            [ "${LINT_CHANGED}" = 'true' ] &&
              printf -- '- `%s`\n' "${LINT_FILE}"
            [ "${MAKEFILE_CHANGED}" = 'true' ] &&
              printf -- '- `%s`\n' "${MAKEFILE}"
            printf 'EOF\n'
          } >>"${GITHUB_OUTPUT}"

      - name: Create GitHub Pull Request
        if: steps.bump-lint.outputs.changed == 'true' || steps.bump-makefile.outputs.changed == 'true'
        id: create-github-pull-request
        uses: craigsloggett/create-github-pull-request@29f3cbd0faff8c1bc2bfebc5cf1598fb8b6f417e # v1.1.0
        with:
          title: 'chore(build): Bump ${{ env.TOOL }} to ${{ env.VERSION }}'
          body: |-
            Bump the pinned `${{ env.TOOL }}` version to `${{ env.VERSION }}` in:
            ${{ steps.changed-files.outputs.files }}
          branch: update/bump-${{ env.TOOL }}-${{ env.VERSION }}
          token: ${{ secrets.GH_TOKEN }}

      - name: Publish Available Updates in Step Summary
        if: steps.bump-lint.outputs.changed == 'true' || steps.bump-makefile.outputs.changed == 'true'
        env:
          FILES: ${{ steps.changed-files.outputs.files }}
        run: |
          # shellcheck disable=SC2016 # Backticks here are literal Markdown code-spans.
          {
            printf -- '### Updates Available\n\n'
            printf -- '`%s` can be updated to `%s` in:\n\n' "${TOOL}" "${VERSION}"
            printf -- '%s\n' "${FILES}"
          } >>"${GITHUB_STEP_SUMMARY}"

      - name: Publish Pull Request Summary
        if: steps.create-github-pull-request.conclusion == 'success'
        # Pinned to the unreleased action HEAD for testing; repin to a release tag once cut.
        uses: craigsloggett/publish-pull-request-summary@f8a8c0651e685ddb88542a5d248673400ab70b71 # unreleased
        with:
          url: ${{ steps.create-github-pull-request.outputs.url }}
